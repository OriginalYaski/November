<!DOCTYPE html>
<html lang="en">
<style>
  .ProjectBox {
    position: relative;
    border: 1px solid blue;
    border-radius: 5px;
    height: 100px;
}
.ProjectButton {
    position: absolute;
    bottom: 5px;
    left: 5px;
}
</style>
  
<body>

<%= link_to "New project", new_project_path %>

<div id="projects">
  <% @projects.each do |project| %>
    <div class="ProjectBox">
    <h3><%= project.title %></h3>
    <p>
        <%= project.description %>
    </p>
    <form class="ProjectButton" action="<%= project_path(project) %>">
        <input  type="submit" value="See this project" />
    </form>
    </div>
  <% end %>
</div>

</body>
</html>
